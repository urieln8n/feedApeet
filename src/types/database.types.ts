// Placeholder — reemplazar con los tipos generados desde el proyecto real de Supabase:
//   npx supabase gen types typescript --project-id <project-id> > src/types/database.types.ts
// No se han generado tipos reales todavía porque no hay un proyecto de Supabase conectado.

export type Database = Record<string, unknown>;
