# server/actions

Server Actions de Next.js organizadas por dominio (`partners.ts`, `refugios.ts`, `impacto.ts`, etc.). Se añaden a medida que se implementa cada paso del orden de implementación — vacío por ahora, solo estructura reservada.
