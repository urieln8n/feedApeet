export default function AdminRefugiosPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-10">
      <h1 className="text-xl font-semibold">Refugios (admin)</h1>
      <p className="text-muted-foreground mt-2 text-sm">
        Alta y edición de refugios y casos — pendiente de implementar (paso 4
        del orden de implementación).
      </p>
    </main>
  );
}
