export default function AdminHomePage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-10">
      <h1 className="text-xl font-semibold">Backoffice — equipo fundador</h1>
      <p className="text-muted-foreground mt-2 text-sm">
        Área restringida a role = admin (RLS). Gestión de refugios, partners,
        atribuciones de comisión e impacto — pendiente de implementar.
      </p>
    </main>
  );
}
