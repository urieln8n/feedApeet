export default function AdminOfertasPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-10">
      <h1 className="text-xl font-semibold">Ofertas (admin)</h1>
      <p className="text-muted-foreground mt-2 text-sm">
        Alta y edición de ofertas por partner (descuento, comisión,
        vigencia) — pendiente de implementar.
      </p>
    </main>
  );
}
