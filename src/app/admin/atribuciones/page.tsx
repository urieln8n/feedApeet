export default function AdminAtribucionesPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-10">
      <h1 className="text-xl font-semibold">Atribuciones de compra (admin)</h1>
      <p className="text-muted-foreground mt-2 text-sm">
        Confirmación manual de compras del marketplace: importe, ahorro del
        usuario y comisión generada, a partir del partner_link usado —
        pendiente de implementar (paso 7 del orden de implementación).
      </p>
    </main>
  );
}
