export default function LoginPage() {
  return (
    <main className="mx-auto flex min-h-svh max-w-sm flex-col items-center justify-center px-6">
      <h1 className="text-xl font-semibold">Entrar a FeedAPet</h1>
      <p className="text-muted-foreground mt-2 text-sm">
        Autenticación con Supabase (magic link) — pendiente de implementar en
        el siguiente paso del orden de implementación.
      </p>
    </main>
  );
}
